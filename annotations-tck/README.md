# Jakarta Annotations TCK Runner

This is the configuration to execute Annotations TCK.

## Prerequisite

Required property:

`payara.home` : path to payara installation

## Test Execution

execute maven test:

```
mvn verify -Dpayara.home=pathtoyourpayara6server
```