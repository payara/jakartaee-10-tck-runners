<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>fish.payara.jakarta.tests.tck</groupId>
        <artifactId>tck-download</artifactId>
        <version>1.0-SNAPSHOT</version>
    </parent>

    <artifactId>jakarta-jsonp-tck</artifactId>

    <packaging>pom</packaging>

    <name>TCK: Install Jakarta JSON-P TCK</name>

    <properties>
        <tck.tests.jsonp.url>https://download.eclipse.org/jakartaee/jsonp/2.1/jakarta-jsonp-tck-${jakarta.tck.jsonp.version}.zip</tck.tests.jsonp.url>
    </properties>

    <build>
        <plugins>
            <plugin>
                <groupId>com.googlecode.maven-download-plugin</groupId>
                <artifactId>download-maven-plugin</artifactId>
                <executions>
                    <execution>
                        <id>download-jsonp-tck</id>
                        <phase>generate-resources</phase>
                        <goals>
                            <goal>wget</goal>
                        </goals>
                    </execution>
                </executions>
                <configuration>
                    <url>${tck.tests.jsonp.url}</url>
                    <unpack>true</unpack>
                    <outputDirectory>${project.build.directory}</outputDirectory>
                </configuration>
            </plugin>
            <plugin>
                <artifactId>maven-install-plugin</artifactId>
                <executions>
                    <execution>
                        <id>install-jsonp-tck-pom</id>
                        <phase>process-resources</phase>
                        <goals>
                            <goal>install-file</goal>
                        </goals>
                        <configuration>
                            <file>${project.build.directory}${file.separator}jsonp-tck${file.separator}artifacts${file.separator}jakarta.json-tck-${jakarta.tck.jsonp.version}.pom</file>
                            <groupId>jakarta.json</groupId>
                            <artifactId>jakarta.json-tck</artifactId>
                            <version>${jakarta.tck.jsonp.version}</version>
                            <packaging>pom</packaging>
                        </configuration>
                    </execution>
                    <execution>
                        <id>install-jsonp-tck-common</id>
                        <phase>process-resources</phase>
                        <goals>
                            <goal>install-file</goal>
                        </goals>
                        <configuration>
                            <file>${project.build.directory}${file.separator}jsonp-tck${file.separator}artifacts${file.separator}jakarta.json-tck-common-${jakarta.tck.jsonp.version}.jar</file>
                            <groupId>jakarta.json</groupId>
                            <artifactId>jakarta.json-tck-common</artifactId>
                            <version>${jakarta.tck.jsonp.version}</version>
                            <packaging>jar</packaging>
                            <sources>${project.build.directory}${file.separator}jsonp-tck${file.separator}artifacts${file.separator}jakarta.json-tck-common-${jakarta.tck.jsonp.version}-sources.jar</sources>
                            <pomFile>${project.build.directory}${file.separator}jsonp-tck${file.separator}artifacts${file.separator}jakarta.json-tck-common-${jakarta.tck.jsonp.version}.pom</pomFile>
                        </configuration>
                    </execution>
                    <execution>
                        <id>install-jsonp-tck-tests</id>
                        <phase>process-resources</phase>
                        <goals>
                            <goal>install-file</goal>
                        </goals>
                        <configuration>
                            <file>${project.build.directory}${file.separator}jsonp-tck${file.separator}artifacts${file.separator}jakarta.json-tck-tests-${jakarta.tck.jsonp.version}.jar</file>
                            <groupId>jakarta.json</groupId>
                            <artifactId>jakarta.json-tck-tests</artifactId>
                            <version>${jakarta.tck.jsonp.version}</version>
                            <packaging>jar</packaging>
                            <sources>${project.build.directory}${file.separator}jsonp-tck${file.separator}artifacts${file.separator}jakarta.json-tck-tests-${jakarta.tck.jsonp.version}-sources.jar</sources>
                            <pomFile>${project.build.directory}${file.separator}jsonp-tck${file.separator}artifacts${file.separator}jakarta.json-tck-tests-${jakarta.tck.jsonp.version}.pom</pomFile>
                        </configuration>
                    </execution>
                    <execution>
                        <id>install-jsonp-tck-tests-pluggability</id>
                        <phase>process-resources</phase>
                        <goals>
                            <goal>install-file</goal>
                        </goals>
                        <configuration>
                            <file>${project.build.directory}${file.separator}jsonp-tck${file.separator}artifacts${file.separator}jakarta.json-tck-tests-pluggability-${jakarta.tck.jsonp.version}.jar</file>
                            <groupId>jakarta.json</groupId>
                            <artifactId>jakarta.json-tck-tests-pluggability</artifactId>
                            <version>${jakarta.tck.jsonp.version}</version>
                            <packaging>jar</packaging>
                            <sources>${project.build.directory}${file.separator}jsonp-tck${file.separator}artifacts${file.separator}jakarta.json-tck-tests-pluggability-${jakarta.tck.jsonp.version}-sources.jar</sources>
                            <pomFile>${project.build.directory}${file.separator}jsonp-tck${file.separator}artifacts${file.separator}jakarta.json-tck-tests-pluggability-${jakarta.tck.jsonp.version}.pom</pomFile>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

</project>